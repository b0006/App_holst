var parser = require( '../' ),
    inspect = require( 'util' ).inspect;

// console.log( inspect( parser.parse( '<svg><svg:g></svg:g><line /></svg>'), { depth: Infinity, color: true } ) );
console.log( inspect( parser.parse( '<line></line><text>Hello</text>'), { depth: Infinity, color: true } ) );
